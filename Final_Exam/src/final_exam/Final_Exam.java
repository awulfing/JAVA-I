package final_exam;
public class Final_Exam {

    public static void main(String[] args) {
        Node temp;
        temp=first.next();
        for(int select=0; x<select;x++)}
            
            if(temp.next==first){
                return temp;
            }
            temp=temp.next();
        }
        return temp;
}
